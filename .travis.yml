language: python

matrix:
    include:
        - os: linux
          services: docker
          dist: trusty
          before_install:
          - docker build -t bubblesub .
          script:
          - docker run -ti bubblesub
